module.exports = () => {
  console.log(`
    '1 nickname' - fetch user and write him in the database;
    '2' - get all users from database;
    '3 location_name' - get all users with given location;
    or '3 null' - to get all users with null location;
`);
};
